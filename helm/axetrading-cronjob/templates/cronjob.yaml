{{- if and .Values.cronJob.create }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "axetrading-cronjob.fullname" . }}
  labels:
    {{- include "axetrading-cronjob.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronJob.cronJobSchedule }}
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ include "axetrading-cronjob.fullname" . }}-cronjob
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              args : {{ .Values.cronJob.cronJobCommands }}
              volumeMounts:
               {{- if .Values.persistence.enabled }}
                - name: {{ include "axetrading-cronjob.fullname" . }}-data
                  mountPath: {{ .Values.persistence.mountPath }}
               {{- end }}
               {{- if .Values.secretsStore.enabled }}
                - name: {{ include "axetrading-cronjob.fullname" . }}-secrets-store
                  mountPath: /mnt/secrets/
                  readOnly: true
               {{- end }}
          restartPolicy: Never
          serviceAccountName: {{ include "axetrading-cronjob.serviceAccountName" . }}-cron
          volumes:
          {{- if .Values.persistence.enabled }}
          - name: {{ include "axetrading-cronjob.fullname" . }}-data
            persistentVolumeClaim:
              claimName: {{ include "axetrading-cronjob.fullname" . }}-pvc
          {{- end }}
          {{- if .Values.secretsStore.enabled }}
          - name: {{ include "axetrading-cronjob.fullname" . }}-secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "axetrading-cronjob.fullname" . }}-secrets
          {{- end }}
{{- end }}